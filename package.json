{
  "name": "bcrypt-password",
  "version": "1.0.2",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (https://kemitchell.com)",
  "dependencies": {
    "bcrypt": "^3.0.6"
  },
  "description": "bcrypt password hash and check functions",
  "devDependencies": {
    "chai": "^4.2.0",
    "check-versions": "^1.0.0",
    "jscs": "^3.0.7",
    "jshint": "^2.6.0",
    "mocha": "^6.1.4"
  },
  "license": "Apache-2.0",
  "main": "bcrypt-password.js",
  "repository": "kemitchell/bcrypt-password.js",
  "scripts": {
    "pre-commit": "npm run lint && npm run test",
    "lint": "check-versions --check-module && jscs bcrypt-password.js test && jshint bcrypt-password.js test",
    "test": "NODE_ENV=test mocha",
    "travis-ci": "npm run pre-commit"
  }
}
